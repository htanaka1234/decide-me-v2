[pytest]
testpaths = tests
markers =
    unit: fast unit tests
    integration: integration tests
    evaluation: scenario evaluation tests
    slow: long-running tests
    phase_gate: required tests for release readiness
    smoke: end-to-end smoke tests
